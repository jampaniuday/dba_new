CREATE TABLE GS_HISTOGRAMS
(
  APP               VARCHAR2(30) NOT NULL,
  TABLE_NAME        VARCHAR2(30) NOT NULL,
  COLUMN_NAME       VARCHAR2(30) NOT NULL
)
/

GRANT SELECT ON GS_HISTOGRAMS TO PUBLIC
/

begin
  EXECUTE IMMEDIATE 'CREATE PUBLIC SYNONYM GS_HISTOGRAMS FOR GS_HISTOGRAMS';
exception
  when OTHERS then
    null;
end;
/
